name: flutter_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  flutter_dotenv: ^5.1.0
  supabase_flutter: ^1.0.0

  cupertino_icons: ^1.0.8

  image_picker: ^1.0.7
  file_picker: ^8.0.0

  excel: ^2.0.0

  path_provider: ^2.1.2
  file_saver: ^0.2.12

  share_plus: ^7.2.0

  intl: ^0.19.0

  sign_in_with_apple: ^6.1.0

  # ⭐ Fix for your current build failure
  webview_flutter: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - .env
